package com.example.activityservice.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.data.mongodb.config.EnableMongoAuditing;

@Configuration
@EnableMongoAuditing
public class MongoConfig {
    // This class can be used to configure MongoDB settings if needed in the future.
    // Currently, it serves as a placeholder for potential MongoDB configuration.

    // You can add MongoDB-related beans or properties here if required.
    // For example, you might want to configure a MongoTemplate or MongoClient if needed.
    // For now, it is empty and serves as a marker for MongoDB configuration.
    // You can also use application.properties or application.yml for basic MongoDB configuration.

    // Example:

}
